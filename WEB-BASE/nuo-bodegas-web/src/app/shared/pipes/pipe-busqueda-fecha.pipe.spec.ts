import { PipeBusquedaFechaPipe } from './pipe-busqueda-fecha.pipe';

describe('PipeBusquedaFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeBusquedaFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
